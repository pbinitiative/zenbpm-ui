# ===========================================
# Live Mode - All endpoints call real backend
# ===========================================
# Inherits from .env, only overrides what's different

VITE_API_STRATEGY=live

# Enable API validation in development
VITE_API_VALIDATION=true
